web:
  build: .
  links:
   - mongodb
  environment:
    VIRTUAL_HOST: loft.airytail.co
  restart: always
mongodb:
  image: mongo
  command: mongod --smallfiles
  restart: always
  volumes:
    - ./mongodb:/data/db
